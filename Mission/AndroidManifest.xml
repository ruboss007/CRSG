<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mission"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="10"
        android:targetSdkVersion="10" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" android:killAfterRestore="false">
        <activity
            android:name="com.example.mission.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="MenuActivity"></activity>
        <activity android:name="ShortActivity"></activity>
        <activity android:name="MissionActivity"></activity>
        <activity android:name="ItemActivity"></activity>
        <activity android:name="MediumActivity"></activity>
        <activity android:name="LongActivity"></activity>
        <activity android:name="SettingsActivity"></activity>
        <service android:process=":myservice" android:name="MService"></service>
    </application>

</manifest>
